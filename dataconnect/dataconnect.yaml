specVersion: "v1"
serviceId: "langveta-client"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "langveta-client-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
